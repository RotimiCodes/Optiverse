<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.optimusbank.optiverseetoken">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OptiverseeToken"
        >


        <activity
            android:name="com.optimusbank.optiverseetoken.ui.Welcome.WelcomeActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:theme="@style/Theme.OptiverseeToken.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.optimusbank.optiverseetoken.MainActivity"
            android:label="test"
           />

        <activity
            android:name="com.optimusbank.optiverseetoken.ui.UserType.usertype_activity"
            android:label="test"
            />


        <activity android:name="com.optimusbank.optiverseetoken.ui.Splash.splash_activity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />


    </application>

</manifest>